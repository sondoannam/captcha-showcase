import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // Add this to suppress .env file warnings (optional)
  reactStrictMode: true,
};

export default nextConfig;
